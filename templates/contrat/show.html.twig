{% extends 'base.html.twig' %}

{% block title %}Contrat{% endblock %}

{% block body %}
    <h1>Contrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contrat.id }}</td>
            </tr>
            <tr>
                <th>DateContart</th>
                <td>{{ contrat.dateContart ? contrat.dateContart|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DureeContart</th>
                <td>{{ contrat.dureeContart }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ contrat.image }}</td>
            </tr>
            <tr>
                <th>TauxProfit</th>
                <td>{{ contrat.tauxProfit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contrat_index') }}">back to list</a>

    <a href="{{ path('app_contrat_edit', {'id': contrat.id}) }}">edit</a>

    {{ include('contrat/_delete_form.html.twig') }}
{% endblock %}
